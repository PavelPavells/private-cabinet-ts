import React from 'react';

const Pagination: React.FC<any> = (): any => {
    return (
        <div className="pagination">
            <div className="pagination__block">PAGINATION</div>
        </div>
    );
};

export default Pagination;
